# Hello

```bash
uv run pytest -v
```

Run the last failure

```bash
uv run pytest -v --lf
```

Run with args

```bash
uv run python argv.py -g Hey -n Python
```

Run smoke tests

```bash
uv run pytest -m smoke
```
